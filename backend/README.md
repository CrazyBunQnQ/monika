# Monika Backend

AI-driven CoC 7e TRPG Platform Backend

## Setup

```bash
# Install dependencies
uv sync

# Run development server
uv run python -m uvicorn src.main:app --reload

# Run tests
uv run pytest
```
